data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
#+INPUT2
+INPUT3
+INPUT4,
control = rpart.control(minsplit = 90, minbucket = 30,
cp = 0.000000003),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
#+INPUT3
+INPUT4,
control = rpart.control(minsplit = 90, minbucket = 30,
cp = 0.000000003),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1*2
+INPUT2
+INPUT3
+INPUT4,
control = rpart.control(minsplit = 90, minbucket = 30,
cp = 0.000000003),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
~INPUT1
~INPUT2
~INPUT3
~INPUT4,
control = rpart.control(minsplit = 90, minbucket = 30,
cp = 0.000000003),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT1
+INPUT2
+INPUT3
+INPUT4,
control = rpart.control(minsplit = 900, minbucket = 300,
cp = 0.000000003),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
blackbox_train <- read.csv("BlackBoxtrainApril22.csv", stringsAsFactors = TRUE)
summary(blackbox_train)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT2
+INPUT3
+INPUT4,
control = rpart.control(minsplit = 900, minbucket = 300,
cp = 0.000000003),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT3
+INPUT3
+INPUT4,
control = rpart.control(minsplit = 900, minbucket = 300,
cp = 0.000000003),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT3
+INPUT3
+INPUT4,
control = rpart.control(minsplit = 90, minbucket = 30,
cp = 0.000000003),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT3
+INPUT3
+INPUT3
+INPUT4,
control = rpart.control(minsplit = 90, minbucket = 30,
cp = 0.000000003),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT2
+INPUT2
+INPUT3
+INPUT4,
control = rpart.control(minsplit = 90, minbucket = 30,
cp = 0.000000003),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT1
+INPUT1
+INPUT2
+INPUT3
+INPUT4,
control = rpart.control(minsplit = 90, minbucket = 30,
cp = 0.000000003),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT1
+INPUT1
+INPUT2^2
+INPUT3
+INPUT4,
control = rpart.control(minsplit = 90, minbucket = 30,
cp = 0.000000003),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT3
+INPUT4,
control = rpart.control(minsplit = 45, minbucket = 15,
cp = 0.000000003),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT3
+INPUT4,
control = rpart.control(minsplit = 180, minbucket = 60,
cp = 0.000000003),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT3
+INPUT4,
control = rpart.control(minsplit = 120, minbucket = 45,
cp = 0.000000003),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT3
+INPUT4,
control = rpart.control(minsplit = 120, minbucket = 40,
cp = 0.000000001),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT3
+INPUT4,
control = rpart.control(minsplit = 360, minbucket = 120,
cp = 0.000000001),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT3
+INPUT4,
control = rpart.control(minsplit = 90, minbucket = 90,
cp = 0.05),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT3
+INPUT4,
control = rpart.control(minsplit = 3000, minbucket = 1000,
cp = 0.05),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT3
+INPUT4,
control = rpart.control(minsplit = 300, minbucket = 100,
cp = 0.05),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT3
+INPUT4,
control = rpart.control(minsplit = 300, minbucket = 100,
cp = 0.05),
parms = list(prior=c(0.65, 0.35), split="SWITCH",
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT3
+INPUT4,
control = rpart.control(minsplit = 90, minbucket = 30,
cp = 0.05),
parms = list(prior=c(0.65, 0.35),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT3
+INPUT4,
control = rpart.control(minsplit = 90, minbucket = 30,
cp = 0.05),
parms = list(prior=c(0.25, 0.55),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT3
+INPUT4,
control = rpart.control(minsplit = 90, minbucket = 30,
cp = 0.05),
parms = list(prior=c(0.25, 0.55,0.75),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT3
+INPUT4,
control = rpart.control(minsplit = 90, minbucket = 30,
cp = 0.000000003),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT3
+INPUT4
+ID,
control = rpart.control(minsplit = 90, minbucket = 30,
cp = 0.000000003),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT3
+INPUT4
+ID,
control = rpart.control(minsplit = 90, minbucket = 30,
cp = 0.000000005),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT3
+INPUT4,
#+ID,
control = rpart.control(minsplit = 90, minbucket = 30,
cp = 0.000000003),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT3
+INPUT4
+ID,
control = rpart.control(minsplit = 90, minbucket = 30,
cp = 0.000000001),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT3
+INPUT4
+ID,
control = rpart.control(minsplit = 90, minbucket = 45,
cp = 0.000000003),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT3
+INPUT4
+ID,
control = rpart.control(minsplit = 45, minbucket = 15,
cp = 0.000000003),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT3
+INPUT4
+ID,
control = rpart.control(minsplit = 300, minbucket = 100,
cp = 0.000000003),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT3
+INPUT4
+ID,
control = rpart.control(minsplit = 150, minbucket = 50,
cp = 0.00000005),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
blackbox_train <- read.csv("BlackBoxtrainApril22.csv", stringsAsFactors = FALSE)
summary(blackbox_train)
blackbox_train <- read.csv("BlackBoxtrainApril22.csv", stringsAsFactors = FALSE)
summary(blackbox_train)
blackbox_train <- read.csv("BlackBoxtrainApril22.csv", stringsAsFactors = TRUE)
summary(blackbox_train)
treeA <- rpart(SOUND~SWITCH
+INPUT1*(49.98)
+INPUT2*(49.74)
+INPUT3*(50.06)
+INPUT4*(49.61)
+ID,
control = rpart.control(minsplit = 150, minbucket = 50,
cp = 0.00000001),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT3
+INPUT4
+ID,
control = rpart.control(minsplit = 120, minbucket = 40,
cp = 0.00000001),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT3
+INPUT4
+ID,
control = rpart.control(minsplit = 60, minbucket = 20,
cp = 0.000000002),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT3
+INPUT4
+ID,
control = rpart.control(minsplit = 60, minbucket = 20
),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT3
+INPUT4
+ID,
control = rpart.control(minsplit = 60, minbucket = 20,
cp = 0.00000000001),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT3
+INPUT4
+ID,
control = rpart.control(minsplit = 60, minbucket = 20,
cp = 0.000000002),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT3
+INPUT4
+ID,
control = rpart.control(minsplit = 90, minbucket = 30,
cp = 0.000000002),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT3
+INPUT4
+ID,
control = rpart.control(minsplit = 75, minbucket = 25,
cp = 0.000000001),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT3
+INPUT4
+ID,
control = rpart.control(minsplit = 60, minbucket = 20,
cp = 0.000000001),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT3
+INPUT4
+ID,
control = rpart.control(minsplit = 60, minbucket = 20,
cp = 0.000000002),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT3
+INPUT4,
control = rpart.control(minsplit = 90, minbucket = 30,
cp = 0.000000003),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT3
+INPUT4,
control = rpart.control(minsplit = 90, minbucket = 30,
cp = 0.000000003, maxdepth = 9),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
treeA <- rpart(SOUND~SWITCH
+INPUT1
+INPUT2
+INPUT3
+INPUT4
+ID,
control = rpart.control(minsplit = 90, minbucket = 30,
cp = 0.000000003, maxdepth = 9),
data = blackbox_train)
rpart.plot(treeA)
CrossValidation::cross_validate(blackbox_train, treeA, 5, 0.8)
blackbox_train <- read.csv("BlackBoxtrainApril22.csv", stringsAsFactors = FALSE)
decision_r <-predict(treeA, newdata = blackbox_test, type = "class")
blackbox_submit$SOUND <-decision_r
write.csv(blackbox_submit, "asilva_blackbox_submit.csv", row.names = FALSE)
summary(blackbox_train)
nrow(blackbox_train)
